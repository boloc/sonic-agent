services:
  frps:
    image: ghcr.io/fatedier/frps:v0.68.1
    container_name: frps
    restart: always
    network_mode: host
    volumes:
      - ./frps.toml:/etc/frp/frps.toml:ro
    command: -c /etc/frp/frps.toml
